# Archive

Superseded planning artifacts, retained for history. Nothing here is current.

- **`plans/`** — the former `docs/plans/`: feature-integration plans
  (agent-capability, frontend, backend-outstanding, alpha-remaining-gaps,
  document-editor-backend) whose work has landed.
- **`checklists/`** — the former `docs/checklists/`: the live progress trackers
  that paired with those plans.
- **`architecture/`** — superseded architecture docs. `overview.md` was replaced
  by [`docs/architecture/runtime-model.md`](../architecture/runtime-model.md),
  the canonical as-built description.

Current design and planning live under [`docs/superpowers/`](../superpowers/)
(`specs/` for designs, `plans/` for step-by-step implementation plans). For how
the code works now, see [`docs/architecture/`](../architecture/); for why it got
that way, [`docs/records/`](../records/).
