# analysis

A chart definition over the project's variables, and what comes back when it is
run.

**Nothing about a result is stored.** A result is what running the definition
produced at a moment; storing one would make the chart and its numbers two things
that can disagree.

Serves `docs/screen-panel-views/{context,inspector}/analysis/`.

**It answers from sample rows.** No `api/` yet.
