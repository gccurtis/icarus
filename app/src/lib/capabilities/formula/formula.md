# formula

The formula engine's built-in vocabulary.

A capability rather than a constant, so the list the Function Builder shows and
the list the evaluator honours cannot drift apart.

**It answers nothing yet.** The views that call it name what it owes them — and
this is the one capability whose real answer needs no stored state, so it is a
candidate to become representation behaviour a panel imports directly.
