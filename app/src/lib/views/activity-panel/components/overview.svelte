<script lang="ts">
  /**
   * What the `overview` activity shows.
   *
   * The one entry in the panel's key map, and the reason the map is total today.
   * It has no content of its own yet — a project's resource tree is the obvious
   * candidate and needs a capability that does not exist. What it does prove is
   * that the rail's choice resolves to a component rather than to a branch.
   */
</script>

<p class="empty">Nothing to show here yet.</p>

<style>
  .empty {
    font-size: var(--token-text-body-sm);
    color: var(--token-ink-muted);
    margin: 0;
  }
</style>
