# Agents reference shared

One constructor, provided through context by the page shell.

`note-log.svelte.ts` holds the notes read from the log and appends new ones
through the view's route. It dies with the mount that made it.
