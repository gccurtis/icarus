# Semantic Overlay components

`implementation.svelte` is the file-level execution plan paired with the
architecture contract in the surface root. It visualizes only observed branch
state and explicitly labels future Jina, index/query, and Derived Output work.
