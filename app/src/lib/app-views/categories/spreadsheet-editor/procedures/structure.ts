/** Default track geometry and the size of one appended batch. */
export const DEFAULT_ROW_HEIGHT = 26;
export const DEFAULT_COLUMN_WIDTH = 112;
export const APPEND_ROWS = 20;
export const APPEND_COLUMNS = 50;
