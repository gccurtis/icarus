node_modules/
dist/
build/
.svelte-kit/
.pnpm-store/
.env
.env.*
!.env.example
.vite/
coverage/
.DS_Store
*.tsbuildinfo
logs/*.log

# Runtime artifacts. One embedded PostgreSQL directory per project, tens of
# megabytes each — state a run produces, never state a checkout needs.
app/data/
app/logs/*.log

# Agent worktrees
.claude/worktrees/

# Agent scratch: subagent-driven-development ledgers, briefs, and review packages
.superpowers/

# Nothing is pinned: lockfiles are regenerable local caches, not committed state.
flake.lock
pnpm-lock.yaml

# Local configuration overrides — real secrets live here
*.local.yaml
**/configuration/local.yaml
