<script lang="ts">
  /**
   * Stand-in content view. Exists so the rail-to-view machinery is complete and
   * exercisable before any real activity is built.
   *
   * Delete this once every activity has a real view.
   */
</script>

<p class="placeholder">Nothing here yet.</p>

<style>
  .placeholder {
    font-size: var(--text-body-sm);
    color: var(--ink-muted);
  }
</style>
