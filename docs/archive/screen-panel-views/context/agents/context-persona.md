# Context

| View | What it is for | Sections |
| --- | --- | --- |
| Context | What this agent can look up | It can look up · Contents |

Retrievable material, as opposed to the prompt material in Behaviour.

## Layout

| 300px |
| --- |
| it can look up |
| contents |
| contents |
| footer |

## It can look up

The Context assigned to this persona, with what it resolves to now.

**Shows** — *Field reports 2024–25* — 96 resources

**Needs** — the persona's `ResourceSet` reference with a live count.

## Contents

A sample of what is actually in it, so a scope that has drifted is visible from
the profile rather than only from the Context screen.

**Shows** — *storm-log-2026-01.csv*, *feeder-12-relay.pdf*, *Ward 3 undergrounding
report* · of 96

**Needs** — a bounded resolve.

**Open** — a Context that matches nothing is unsafe while an empty scope is
treated as whole-project: an agent configured to see almost nothing would silently
see everything. Warn or block until explicit-empty is distinguishable.

## Panel furniture

**Open Context screen** at the foot.
