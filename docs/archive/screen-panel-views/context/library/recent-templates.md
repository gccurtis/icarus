# Recent

| View | What it is for | Sections |
| --- | --- | --- |
| Recent | What has changed lately and what has been used lately | Recently updated · Recently used |

Two different questions that a single "recent" list would conflate. A template
you edited yesterday and a template you used yesterday are different signals.

## Layout

| 300px |
| --- |
| recently updated |
| recently used |

## Recently updated

**Shows** — *Regulatory filing shell* — 2w; *Board update* — 3w

**Needs** — `updatedAt` on `Template`.

## Recently used

**Shows** — *Cost model skeleton* — today

**Needs** — a record of instantiation. Nothing currently counts uses, and the
resource made from a template records its origin — so this list is a reverse
query over resources rather than a counter on the template.

**Open** — that reverse query has to exist, or this section cannot be built.
