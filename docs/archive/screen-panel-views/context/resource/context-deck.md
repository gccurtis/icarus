# Context

| View | What it is for | Sections |
| --- | --- | --- |
| Context | What the prompt blocks in this deck can look up | Saved Contexts |

The same view as the document editor's, for the same reason: a generated block
runs against a scope, and the scope is worth seeing without leaving the deck.

## Layout

| 300px |
| --- |
| saved contexts |
| footer |

## Saved Contexts

**Shows** — *Field reports 2024–25* — 96; *Regulatory corpus* — 34

**Needs** — `ResourceSet` records with live resolved counts.

**Open** — the same question as in the document: whether this lists the scopes
actually in use here, or every scope so one can be assigned.

## Panel furniture

**Open Context screen** at the foot.
