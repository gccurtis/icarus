# Project Overview Notes

The work section should be called `resources` instead.

The header on overflow should click to reveal a dropdown so the user can select all the people.

The review section should have a subtitle just like the create section. The buttons for mentions and activity should be on the right side and should be toggles.

The resource table should be scrollable. The sort dropdown should not have the sort icon because it is redundant. The sort toggle and toggle should be on a surface so it is clear they are related.

Update the spec for the threads. Also do not freeze any of the clicks and update the briefs.

We need inspector panel views for files, findings, and research threads.


Fix the inspector panel issues, remember the inspector panel should pull the information it needs conditioned on the label that is set.
