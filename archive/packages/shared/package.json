{
  "name": "@icarus/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsc -w --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
