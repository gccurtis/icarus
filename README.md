# Icarus

Reset in progress. The root is intentionally bare — there is no workspace, build, or test command
here yet.

The previous system is preserved under `archive/` for reference. Nothing was deleted; every file
keeps its history at its original path.
